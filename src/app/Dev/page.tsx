// DEV
'use client'

import Form from "../form"

export default function DevPage() {
    return <Form/>
}
// the old landing page's dev nav (MobileTester / GAME / SignUp buttons),
// moved here when '/' became the redirect to the player's island. Not in
// the proxy matcher, so it opens logged in or out
